apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-commerce.fullname" . }}-test-connection"
  labels:
    {{- include "test-commerce.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-commerce.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
